# Fase 3E — Plano de implementação

**Objetivo:** priorizar tomadas com componentes explicáveis e comparar possíveis
redundâncias no mesmo contexto, preservando mídia e decisões humanas.
**Spec:** `Docs/03_FASE_3_INTELIGENCIA_EDITORIAL_E_ORGANIZACAO_AUTOMATICA.md`, seção 12.
**Arquitetura:** funções locais com Pillow e telemetria existente; snapshots auditáveis
em jobs `CALCULATE_SCORE`; tabela de perfis editáveis. Sem dependências novas.

- [x] Calcular technical (exposição/nitidez de thumbnail), motion (variação de
  velocidade angular/aceleração), duration e people. Composição permanece nula
  sem evidência; nunca substituir ausências por zero. Normalizar pesos disponíveis
  e mostrar cobertura. Testar falta de dados, zeros, yaw circular e tempo inválido.
- [x] Comparar hashes perceptuais apenas entre Trip + grupo confirmado + movimento
  final + assunto, com tipo de mídia igual. Sem contexto completo, não inferir
  equivalência. Testar isolamento, imagens uniformes e candidatos sem thumbnail.
- [x] Persistir perfis Instagram/YouTube e snapshots completos de cada execução
  em jobs existentes, com versão, evidência, pesos e dados de entrada. Reprocessar
  sem alterar classificações. Testar migração, histórico, falha, reinício, cliques
  duplicados e alterações posteriores de contexto (resultado obsoleto).
- [x] Expor API autenticada/CSRF e painel de ranking, edição de pesos, componentes,
  cobertura e evidências. Manter seleção sequencial da grade. Testar API e script,
  conferir visualmente com dados sintéticos locais.
- [x] Executar suíte pelo wrapper Windows, Ruff e mypy; revisar diff.

## Verificação

- 354 testes aprovados e 3 ignorados pelo wrapper Windows, usando a `.venv`
  existente com `UV_NO_SYNC=1` (PyPI indisponível no sandbox).
- `ruff check .` e `mypy src`: aprovados. Formatação dos 11 arquivos Python
  alterados: aprovada. A checagem global aponta formatação preexistente fora do escopo.
- Navegador com catálogo sintético e API real via harness local: cálculo, pesos,
  indicação de desatualização, recálculo e revisão de take semelhante conferidos;
  nenhum erro JavaScript observado.
- Revisão independente encontrou concorrência na criação inicial do perfil;
  teste reproduziu `UNIQUE constraint failed`, serialização da transação corrigiu,
  teste de duas gravações e sequência da auditoria aprovado.
- Decisão: resultados persistidos nos jobs existentes, sem criar tabela duplicada
  de análises. Limite: snapshots por viagem aumentam o banco se reprocessados muitas vezes.

**Limites:** thumbnail único é indício, não avaliação do vídeo inteiro; dHash sugere
semelhança, não duplicação comprovada. Sem calibração em acervo real disponível,
registrar fórmulas e limitações. Scores não apagam, movem ou selecionam mídia.
**Validação Windows:** inicializar `scripts/dev-env.ps1`; testes apenas por
`scripts/pytest-windows.ps1`, sempre com diretório temporário exclusivo.
